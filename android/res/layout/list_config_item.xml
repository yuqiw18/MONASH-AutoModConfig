<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="8dp"
    android:paddingEnd="16dp"
    android:paddingStart="16dp"
    android:paddingTop="8dp">

    <TextView
        android:id="@+id/labelConfigName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/imgConfigImage"
        android:layout_toEndOf="@+id/imgConfigImage"
        android:text="@string/ui_label_default_value"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/labelConfigHighlight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/labelConfigName"
        android:layout_toEndOf="@+id/imgConfigImage"
        android:text="@string/ui_label_default_value" />

    <ImageView
        android:id="@+id/imgConfigImage"
        android:layout_width="@dimen/img128"
        android:layout_height="@dimen/img64"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/ui_image_description_default"
        app:srcCompat="@drawable/img_placeholder_wide" />
</RelativeLayout>